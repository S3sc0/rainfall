#include <stdio.h>
#include <stdlib.h>

void    o(void)
{
        system("/bin/sh");
        exit(1);
}

void    n(void)
{
        char    buff[536];

        fgets(buff, 520, stdin);
        printf(buff);
        exit(1);
}

int     main(void)
{
        n();

        return 0;
}